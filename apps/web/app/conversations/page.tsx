'use client'; import {useEffect,useState} from 'react';import {api} from '../../lib/api'; type C={id:string;display_name:string;ai_mode:'off'|'copilot';last_message_at:string};export default function Conversations(){const[c,setC]=useState<C[]>([]);useEffect(()=>{api<C[]>('/conversations').then(setC)},[]);return <><h1 className="mb-5 text-3xl font-bold">Диалоги</h1><div className="space-y-3">{c.map(x=><a key={x.id} href={`/conversations/${x.id}`} className="card block"><b>{x.display_name||'Telegram chat'}</b><p className="muted">AI: {x.ai_mode==='copilot'?'Copilot':'Off'}</p></a>)}{!c.length&&<div className="card muted">Здесь появятся только чаты, переданные подключённому Business Bot.</div>}</div></>}
