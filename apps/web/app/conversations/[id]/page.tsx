'use client';import {useEffect,useState} from 'react';import {api} from '../../../lib/api';export default function Conversation({params}:{params:Promise<{id:string}>}){const[id,setId]=useState('');const[c,setC]=useState<any>();const[g,setG]=useState<any>();useEffect(()=>{params.then(x=>{setId(x.id);api(`/conversations/${x.id}`).then(setC)})},[params]);if(!c)return <p>Загрузка…</p>;async function mode(){const n=c.ai_mode==='copilot'?'off':'copilot';setC(await api(`/conversations/${id}/ai-mode`,{method:'PATCH',body:JSON.stringify({mode:n})}))}return <div className="space-y-4"><h1 className="text-3xl font-bold">{c.display_name}</h1><button className="button" onClick={mode}>AI: {c.ai_mode==='copilot'?'Copilot':'Off'}</button><div className="card space-y-2">{c.messages.map((m:any)=><p key={m.id}><b>{m.direction==='incoming'?'Она/Он':'Вы'}:</b> {m.text??'Сообщение скрыто: AI был выключен'}</p>)}</div>{c.ai_mode==='copilot'&&<button className="button" onClick={async()=>setG(await api(`/conversations/${id}/suggestions`,{method:'POST'}))}>Предложить 3 ответа</button>}{g?.options.map((o:any)=><section className="card" key={o.id}><b>{o.tone}</b><p className="my-3">{o.text}</p><button className="button" onClick={async()=>{await api(`/conversations/${id}/send`,{method:'POST',headers:{'Idempotency-Key':crypto.randomUUID()},body:JSON.stringify({generation_id:g.generation_id,option_id:o.id})});alert('Отправлено')}}>Отправить</button></section>)}</div>}
