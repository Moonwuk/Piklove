'use client';
import {useEffect,useState} from 'react'; import Link from 'next/link'; import {api} from '../lib/api';
declare global{interface Window{Telegram?:{WebApp:{initData:string;ready():void;expand():void}}}}
export default function Home(){const [connected,setConnected]=useState<boolean|null>(null);useEffect(()=>{(async()=>{const tg=window.Telegram?.WebApp;tg?.ready();tg?.expand();if(tg?.initData)await api('/auth/telegram',{method:'POST',body:JSON.stringify({init_data:tg.initData})});const c=await api<{connected:boolean}>('/telegram/connection');setConnected(c.connected)})().catch(()=>setConnected(false))},[]);return <div className="space-y-4"><h1 className="text-3xl font-bold">AI Copilot</h1><section className="card"><p className="font-semibold">Telegram Business</p><p className={connected?'text-green-600':'muted'}>{connected?'● Подключён':'○ Не подключён'}</p></section>{!connected&&<section className="card"><h2 className="font-bold">Подключите Telegram Business</h2><p className="muted mt-2">AI работает только с чатами, доступными Business Bot и включёнными вами для Copilot. Мы не просим пароль или код Telegram.</p></section>}<section className="card"><p>План: <b>FREE</b></p><p>AI usage: 0 / 20</p></section><Link className="button text-center" href="/conversations">Диалоги</Link><div className="grid grid-cols-2 gap-3"><Link className="card text-center" href="/settings">Настройки</Link><Link className="card text-center" href="/privacy">Privacy</Link></div></div>}
